# 增量开发之-任务编辑对话框

1. 设计日任务的各上层任务:周任务\月任务\年任务(完成)
1. 改造Today类或继承Today类以适应未来的某个时刻(today是不能修改日期的,而这里需要一个能任意修改日期的类)(完成)
1. 设计任务对话框(对话框,应该可移动,Enter键确定,所有表格使用一个对话框(作为构造参数传入))(完成)
1. 设计各个阶段的显示表格,右键弹出菜单,事件处理等(删除任务时如果没有选中任务则不予理睬,删除跟delete键捆绑,删除修改之前判断任务是否可编辑),表格最后一栏统计时间总和,字体渲染(未完成,已完成)...
1. 各阶段表格放置到编辑窗体中(保持表格宽度,就一个表格好办(表格而非包围它的组件的宽度))(每次弹出编辑窗体前要从文件中更新集合)
	2. 实现Updater接口
1. 各阶段表格放置到最大化窗体中(本周\本月\本年 的可以直接添加,还有明天的(这个很重要),保持表格宽度,保持今天的表格的宽度)(今天可能是空行,所以重新布局的时候如果某个表格只有0行(只可能是今天),那么给它一行的高度)
	2. 实现Updater接口
1. 随着日任务的操作,周月年任务可能要刷新,点击标签的刷新与此不同(要分两个函数来操作了),编辑窗体的修改可能会影响到主窗体的内容,这个也需要刷新(默认主窗体只readTasks一次的,那么编辑窗体的修改可能就被覆盖了(很严重的))
1.从内存中遍历任务刷新
2.从文件中读取任务刷新
